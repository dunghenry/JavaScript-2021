// Làm bài tập tại đây

function run(x, y, z) {
    x *= 2;
    y /= 3;
    z %= 2;

    return "x is " + x + " and y is " + y + " and z is " + z;
}
// Làm bài tập tại đây

function decrease(number) {
    number += 2;

    // không sửa phần dưới này
    return number
}