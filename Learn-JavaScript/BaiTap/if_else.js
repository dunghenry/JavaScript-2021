// Làm bài tập tại đây

function run(number) {
    var result;
    if (number % 2 === 0) {
        result = "even";
    } else {
        result = "odd";
    }

    return result;
}
// Làm bài tập tại đây

function checkCanAccessible(age) {
    var isAccessible;

    if (age >= 16) {
        isAccessible = true;
    } else {
        isAccessible = false;
    }

    return isAccessible;
}