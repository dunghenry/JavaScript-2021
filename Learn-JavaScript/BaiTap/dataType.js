// Làm bài tập tại đây
var fullName = "Dung";
var myGirlFriendName = "No";
var currentLanguage = "Javascript";

function run(input) {
    var output;
    if (typeof input === "string") {
        output = true;
    } else {
        output = false;
    }

    return output;
}

var pi = Math.PI.toFixed(2);